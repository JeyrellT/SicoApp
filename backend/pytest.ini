[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    integracion: pruebas que requieren descargar datos reales del origen SICOP
    lento: pruebas que tardan más de 30 segundos
